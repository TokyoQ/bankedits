w = require './wikichanges'

for name, info of w.wikipedias
  url = 'https://' + info.short + '.wikipedia.org'
  name = info.long.replace(' Wikipedia', '')
  console.log '* [' + name + '](' + url + ')'
